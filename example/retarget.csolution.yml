# A solution is a collection of related projects that share same base configuration.
solution:
  description: Standard output retargeting example
  created-for: CMSIS-Toolbox@2.12.0
  cdefault: # use CMSIS-Toolbox default Compiler options

  # Compilers that can be selected
  select-compiler:
    - compiler: AC6
    - compiler: GCC
    - compiler: IAR
    - compiler: CLANG

  # List the packs that define the device and/or board.
  packs:
    - pack: ARM::Cortex_DFP

  # List different targets that are used to deploy the solution.
  target-types:
    - type: FVP_MPS2_CM3
      device: ARMCM3
      variables:
        - Board-Layer: ../board/FVP_MPS2_Cortex-M3/Board.clayer.yml
      target-set:
        - set: "FVP_MPS2_Cortex-M3"
          images:
            - project-context: retarget.Debug
          debugger:
            name: Arm-FVP
            model: FVP_MPS2_Cortex-M3
            config-file: board/FVP_MPS2_Cortex-M3/fvp_config.txt
            args: ""

  # List of different build configurations.
  build-types:
    - type: Debug
      debug: on
      optimize: none

    - type: Release
      debug: off
      optimize: size

  # List related projects.
  projects:
    - project: ./project/retarget.cproject.yml
